<Window x:Class="OctoCut.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="설정"
        Width="560"
        Height="180"
        MinWidth="520"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontWeight="SemiBold"
                   Text="FFmpeg 실행 파일"/>

        <DockPanel Grid.Row="1" Margin="0,8,0,0">
            <Button DockPanel.Dock="Right"
                    Width="80"
                    Margin="8,0,0,0"
                    Content="찾기"
                    Click="Browse_Click"/>
            <TextBox x:Name="FfmpegPathBox"
                     VerticalContentAlignment="Center"/>
        </DockPanel>

        <TextBlock Grid.Row="2"
                   Margin="0,8,0,0"
                   Foreground="#57606A"
                   Text="비워두면 PATH에서 ffmpeg.exe를 자동으로 찾습니다."
                   TextWrapping="Wrap"/>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Width="84"
                    Margin="0,0,8,0"
                    Content="저장"
                    IsDefault="True"
                    Click="Save_Click"/>
            <Button Width="84"
                    Content="취소"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
